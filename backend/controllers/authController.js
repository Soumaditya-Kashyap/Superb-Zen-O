const {
    register,
    login,
    getCurrentUser,
    logout,
    getPreferences,
    updatePreferences
} = require('./auth');

module.exports = {
    register,
    login,
    getCurrentUser,
    logout,
    getPreferences,
    updatePreferences
};

